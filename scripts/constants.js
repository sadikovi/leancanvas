var Constants = {
    COOKIE_NAME = "GITHUB_SADIKOVI_LEAN_CANVAS_TEMP_CONURL";
    COOKIE_EXPIRE_SEC = 365*24*60*60; // cookie is stored for one year
    AUTOSAVE_INTERVAL = 15*60*1000; // autosave interval is every 15 minutes
}
